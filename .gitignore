# Python
__pycache__/
*.pyc
.venv/
venv/

# Trainings-Artefakte (werden lokal erzeugt, nicht eingecheckt)
ausgabe/
daten/raw_*.txt
daten/training.log

# Hinweis: daten/korpus.txt (gemeinfreier Goethe-Text) wird bewusst
# mit eingecheckt, damit das Training ohne Internet sofort startet.

# Lokaler Suchindex (wird aus den Dokumenten des Nutzers erzeugt)
.theo_index.json
